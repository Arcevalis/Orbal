<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- LM Studio remote mode: allow cleartext HTTP to local network / emulator host -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Emulator loopback to host -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Common LAN ranges — user can enter any host IP, but we explicitly allow these -->
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <!-- Allow any private host when user enters custom LAN IP (e.g. 192.168.1.50) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
